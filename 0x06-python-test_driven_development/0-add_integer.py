#!/usr/bin/python3
def add_integer(a, b):
    """Returns a + b.

    Works with integers:

    >>> add_integer(2, 3)
    5
    """
    if isinstance(a, int) and isinstance(b, int):
        return a + b
    return a + b
