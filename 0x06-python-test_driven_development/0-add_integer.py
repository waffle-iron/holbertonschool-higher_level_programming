#!/usr/bin/python3
def add_integer(a, b):
    """Returns a + b.

    Works with integers:

    >>> add_integer(2, 3)
    5
    """
    int(a, b)
    if a is not string:
        return a + b
    return a + b
